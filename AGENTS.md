# Codex 协作说明

本仓库是陆书鸿的长期个人职业档案，目标是让未来在另一台电脑上的 Codex，仅依靠仓库内容就能继续整理项目、准备面试、跟踪求职和最终优化简历。

## 当前阶段

- 当前日期基线：2026-09-05。
- 首要任务：继续核实待补事实，并围绕目标 JD 复习、模拟面试和迭代简历。
- `main.tex` 已于 2026-09-05 完成第一版重写与滴滴项目事实校准；南湖经历仍有待本人确认项。后续修改仍要先过事实台账，并以用户给出的目标 JD 或明确修改要求为边界。
- 不要自动提交或推送。执行 Git commit/push 前必须得到用户明确授权。

## 用户明确偏好

- 项目总结先讲业务问题、个人决策、结果和反思。
- 只保留面试官可能追问、且能体现技术判断的实现细节。
- 不把版本路由、空跑、配置键数量、临时分支和普通 CRUD 细节写进项目总结或简历。
- 指标必须保留口径：样本、时间、对照、计算方法和限制。
- 不夸大。方案设计不能写成已经上线；离线回放不能写成生产 SLA；相关性不能写成因果收益。
- 遇到材料冲突，先写入事实台账并标记待本人确认，不擅自选择更好看的版本。

## 求职主线

主定位：有算法与研究底座、能把不确定模型能力转化为可评测、可约束、可上线系统的 AI 应用 / Agent 工程师。

投递优先级：

1. AI 应用研发、Agent 开发、Agent Engineer；
2. Agent 应用平台或偏运行时的应用型 Agent Infra；
3. 业务机器学习 / Applied ML；
4. 只有 JD 与既有经历高度匹配时，才投以预训练、后训练或底层算子为主体的岗位。

Java/Spring 是生产工程能力证据，不是唯一职业身份；面试编码与 AI 应用主语言优先恢复 Python。

## 长期能力改善主线

第一段互联网工作的领导反馈主要指向：业务上下文获取偏慢、需求复述和主动汇报不足、项目效果回收不完整、风险/依赖推进与 Ownership 不成熟，以及口头表达和 Demo 需要训练。代码方面应表述为“新工程环境适应速度和 AI 辅助验证闭环需要提高”，不要扩大成“代码能力差”。领导同时认可后期 SQL 和交付速度已有改善。

未来 Codex 协作时：

- 项目训练先问用户、业务问题、Baseline、Primary/Guardrail Metric 和效果回看方式，再进入实现；
- 要求回答结论先行，并区分结论、证据、影响、请求、下一步和 ETA；
- 帮助记录风险、依赖、决策和复盘，但不虚构跨团队故事或已经形成的习惯；
- 将 Agent 作品同时作为 Workflow/Eval 技术训练和需求、汇报、Demo、结果闭环训练；
- LeetCode 重视独立完成率、测试、复杂度和 D+1/D+7/D+21 复做，不只追题量；
- 私人反思不能直接复制到简历。行为面应转化为具体情境、行动、影响、改进机制和当前证据。

入口见 `docs/reflection/01-第一段互联网工作复盘.md`、`docs/study/05-三个月能力改善计划.md` 和 `docs/interview/03-行为面与职业反思准备.md`。

## 三个滴滴项目的事实边界

### 全国换电工单时效预测

- 可证明：Label 和时序特征治理、XGBoost 模型迭代、日期 OOT、同工单评估、Java 接入和线上效果分析。
- 核心结果：181 日、7,829,793 行原始数据；318,499 条同工单、228 城公平对照；MAE 126.84 → 123.91，改善 2.30%；196/228 城改善。
- 限制：收益主要来自长时段；短时桶有退化；严格业务目标没有全面达到。
- 安全职责表述：接手既有系统，负责或主导后续模型与工程演进；不要说从零搭建全系统。

### 车辆阈值智能调整系统

- 可证明：自然语言到受控领域命令、实体知识查询、Java 确定性校验、配置预览、BPM 审批、回调执行、审计和外部 Agent 结构化接入。
- 金标评测：42 条、累计 239 次模型调用；最终候选裸通过 40/42，经 Java 后置校验 41/42，0 截断；平均 11.61 秒，P95 20.21 秒。
- 日志回放：43 条；平均延迟 34.43 秒 → 7.17 秒，下降 79.2%；但候选模型一次调用业务可接受为 40/43，不能把速度提升等同于质量全面提升。
- 限制：生产业务量、长期 SLA、真实采纳率和业务收益待补；离线候选参数与后来代码配置存在差异，不能说长期线上达到 97.6%。

### 城市异动诊断 Agent

- 可证明：已有阈值执行底座和下一阶段完整架构设计。
- 当前不能证明：Hive MCP、多步诊断 Workflow、证据绑定、效果反馈已正式上线。
- 对外只能说“设计/规划”，除非未来补到代码、部署、调用日志和效果报告。
- 详细设计的统一入口是 `docs/city-agent/README.md`，其下按现状、产品、LangGraph、数据/工具/模型、评测/SLO、实施/运维拆分；不要把这些文档中的目标数字当实绩。
- 推荐的技术原则是“确定性 Workflow 包住有限 Agent”：统计/规则发现异常，LLM 做有限假设、动作方向与解释，领域工具提供事实，确定性推荐器计算候选值；高风险动作分开经过产品审核、下游 BPM、执行前重验和既有动作服务终态回查。
- 未来实现必须先加固 request/建议版本、Diff、Dry Run、配置版本保护、持久化幂等、可靠消息和执行终态回传，再扩大写权限。

## 文档优先级

- `README.md`：总入口和阅读顺序。
- `docs/00-个人职业画像与求职主线.md`：统一职业叙事。
- `docs/01-滴滴三个项目总览.md`：三项目关系和状态。
- `docs/didi/`：面试可用的项目总结。
- `docs/city-agent/`：城市异动诊断 Agent 下一阶段的详细权威蓝图；它是设计证据，不是上线证据。
- `docs/evidence/01-事实指标与待补证台账.md`：数字与表述的唯一事实闸门。
- `docs/interview/`：STAR 和递进追问。
- `docs/knowledge/`：知识复习和系统设计，不代表项目已实现。
- `docs/market/`：带日期的岗位和面经快照，后续必须注明更新时间。
- `docs/study/` 与 `tracking/`：计划、进度和复盘。
- `docs/archive/02-时效预测资料迁移索引.md`：说明原始长复盘中的信息迁移到了哪里。

## 每次协作流程

1. 先读 `README.md`、本文件和事实台账。
2. 根据任务只读相关项目文档，避免把整仓细节重新堆回摘要。
3. 新增数字时同步更新事实台账，注明证据类型和限制。
4. 新增市场信息时注明检索日期并链接原始来源；过期信息保留为历史快照，不静默当作当前事实。
5. 修改简历前，先从 `docs/resume/` 选定候选表述，并逐条通过事实台账；修改后同步记录当前版本状态。
6. 完成后检查 Markdown 链接、事实冲突、敏感信息和 Git diff；默认不提交、不推送。

## 隐私与知识产权边界

- 不把公司源码、完整 SQL、内部域名、表名、接口地址、密钥、Token、Cookie、员工信息、真实业务样本或未脱敏日志复制进个人仓库。
- 可以保留个人可公开讲述的架构、数据口径、伪代码、脱敏 Schema、聚合指标、实验设计和本人反思。
- 如需保存代码示例，应从零改写成最小通用示例，替换内部命名和真实数据，并确认符合雇主政策及保密义务。
- 如果“可复习价值”和“公司机密风险”冲突，优先保存方法与结论，不保存原始资产。
- 根目录三份原始 SQL 只供本机静态核验，已通过 `/*.sql` 忽略；长期结论已经脱敏写入 `docs/evidence/05-时效数据契约与评估口径.md`，不要提交原 SQL。
- 未脱敏的原始时效长复盘保存在本机 `local-only/` 并被 Git 忽略；长期仓库只使用已经脱敏的项目、指标和数据契约文档。
